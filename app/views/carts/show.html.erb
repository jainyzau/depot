<% if notice %>
    <p id="notice"><%= notice %></p>
<% end %>

<ul>
    <% @cart.line_items.each do |e| %>
        <li><%= e.quantity %> &times; <%= e.product.title %> </li>
    <% end %>
</ul>
<%= link_to 'Back', store_path %>
